---
last-reviewed: 2026-08-05
status: active
---

# 计划 0006：里程碑 0.3 设备仪表盘基础与锁屏交互

## 目标

交付接近盒子屏幕、并与已确认字段语义一致的首页与锁屏目标原型，同时实现共享状态模型、局部降级和来源门槛已经满足的字段。ADR-0005 已批准用户授权的本地只读状态白名单；0.3 按该边界进入 Release 实现。

## 范围门槛

1. 首页卡内容于 2026-08-05 收敛为“我的 FMO、当前服务器、单行动态事件”三块；独立视觉稿确认后再同步首页与锁屏投影。
2. 0.3 可基于现有 GEO 接口开发共享模型、本地会话状态、坐标派生 Maidenhead 和局部降级。
3. 每个新增设备内部字段必须先取得公开契约、接口所有者授权，或像 ADR-0005 一样由用户授权并固定严格最小边界，明确传输、鉴权、字段、单位、更新时间、错误与版本策略。
4. 无正式来源的字段登记为 `Deferred — external contract`，不进入 Release 的 `available` 状态，不用 APRS、QSO 导入、GEO RTT 或 fixture 冒充。
5. 超出 ADR-0005 白名单的抓包观察仍登记为 `Observed — authorization pending`，不能因同一端点已接入而扩大开发范围。

完整设备状态接口仍是完整仪表盘数据的门槛，但不再作为基础状态模型和 GEO 支持字段的总里程碑门槛。

## 信息范围

| 字段 | 最终首页 | 最终锁屏默认 | 当前实施状态与来源 |
|---|---:|---:|---|
| 我的呼号 | 是 | 是，可隐藏 | Implemented — ADR-0005：`user/getInfo.callsign` |
| 当前连接服务器 | 是 | 是 | Implemented — ADR-0005：`station/getCurrent.name/uid`；已观察 A → B → A |
| 过滤器距离 | 是 | 否 | Implemented — ADR-0005：`0...7` 映射禁用/50/100/200/500/1000/2000/5000 km |
| 梅登黑德坐标 | 是 | 否，默认隐藏 | 0.3：由带来源时间的 GEO WGS84 坐标派生 |
| QSO 日志数 | 否，移至 QSO 页 | 否 | Implemented data — ADR-0005：`qso/getList.count`；不是实时通联人数，不投影到首页 |
| 工作频率 | 否，语义待确认 | 否 | Implemented data — ADR-0005：单一 `getUserPhyFreq.freq`；保留数据层，不投影到首页且不得拆成 TX/RX |
| 当前讲话状态 | 是 | 是 | Implemented — ADR-0005：`/events qso/callsign` 的呼号、可选 grid 与讲话状态 |
| 本地最近活动 | 最新一条/入口 | 最新一条 | Implemented — ADR-0005：`/events qso/history`，最多 20 条；与 0.4 APRS 时间线分离 |
| 盒子到服务器延迟 | 隐藏 | 隐藏 | 用户确认 0.3 隐藏；不得以 App 到 GEO RTT 冒充 |
| 服务器管理员呼号 | 有可靠字段时条件显示 | 隐藏 | Deferred — no evidence；当前整行隐藏，`isHost` 样本不得用于推断管理员呼号 |
| 当前 / 最大在线人数 | 隐藏 | 隐藏 | Deferred — no evidence；0.4 STATION 不得冒充当前连接状态 |

## 状态模型

- `DashboardSnapshot` 是首页和 ActivityKit 的唯一规范输入。
- 每个字段包含值、来源、`observedAt`、可信度以及 `available / unknown / stale / unsupported / rejected` 状态。
- GEO、本地设备状态和未来 APRS 连接互相独立；单一来源失败只降级相关字段。
- 连接中断保留最后可信值并显示最后更新时间；不会把缓存值写成“实时”。
- 锁屏只投影白名单字段，动态状态最多一条事件，静态与动态数据合计通过 4 KB 上限测试。
- 本地 `/events qso/callsign` 独立提供有开始/空闲状态的当前讲话者；它不能与 APRS `VOCAL` 的“最近检测到语音活动”模型合并。可见界面优先用扬声器图标、呼号、可选网格和时间表达，不堆叠重复文案。
- Release 依赖组合不包含 demo provider；fixture 只存在于测试、Preview 与 HTML 原型。

## 用户流程

1. 未连接时，首页顶部卡片保持发现与连接入口。
2. 连接成功后，顶部卡片原位切换为仪表盘基础；只呈现已有真实来源的字段，未接入字段不使用原型数值填充。
3. HTML 继续展示最终锁屏形态；正式 App 的实时活动只投影当前可用白名单字段，由用户显式开始。
4. 用户可在设置中关闭锁屏呼号或事件；梅登黑德默认不在锁屏显示。
5. 网络断开时，首页和锁屏进入过期态并显示最后更新时间；恢复后由新快照刷新。
6. 用户可结束实时活动；系统禁用、启动失败或系统结束活动时，首页不受影响。

## 实施顺序

当前进度：共享快照、三条独立链路状态、GEO/Maidenhead、ADR-0005 `/ws` 状态客户端、`/events` 事件流，以及已确认的“呼号与位置/范围、服务器与动态大胶囊”三段式首页投影已经完成并通过单元测试；QSO 数、频率、分隔线、解释性标签和卡片操作均未进入正式投影。真实 FMO 真机验收与 ActivityKit 基础尚未开始。

2026-08-05 使用用户临时提供的可达地址进行脱敏最小探测：`/ws` 五个白名单请求全部返回预期路由和字段类型；`/events` 握手成功但 5 秒窗口内没有事件帧，因此事件值解码仍需在实际讲话/历史事件出现时完成真机验收。仓库不记录该地址，探测没有输出或保存真实字段值。

验证记录：通用 Simulator 构建和完整单元测试通过，新增白名单、大小上限、事件、聚合与生命周期测试通过。首台自动连接的聚焦 XCUITest 已通过；仪表盘场景曾成功到达并识别定稿字段，但随后重跑受本机 Xcode 26.5 `DebuggerVersionStore` / `xctrunner` 启动异常影响，尚未取得稳定的最终通过记录。需在模拟器服务恢复或真机验收时重跑，不能把环境失败记为功能通过。

### 待补观察与真机验证

- [ ] 服务器不可达、恢复与 WebSocket 重连；不把“无服务器”发明成正常选择。
- [ ] `isHost: true` 样本；取得前隐藏主机/管理员角色。
- [ ] FMO 重启后 `/events seq/ts`；取得前重连后重新拉全量快照，不跨会话续排。
- [ ] 若未来发现在线/最大人数或管理员呼号的正式字段，再单独评审并接入。

### 1. 纯模型与延期字段登记

- 实现字段状态、可信度、来源和时间模型。
- 为每个目标字段登记当前来源或 `Deferred — external contract`。
- 用 fixture 覆盖完整、局部、过期、拒绝、长文本和边界值。

### 2. 状态聚合

- 通过协议注入现有 GEO 提供者、未来设备状态提供者、时钟和存储。
- Actor 隔离聚合，生成不可变 `DashboardSnapshot`。
- 确保断线、切换设备、取消和恢复不会串用旧设备状态。

### 3. 首页仪表盘

- 在现有连接卡片内呈现真实可用字段和局部降级。
- 延期字段按布局语义选择不渲染或明确不可用，不显示 `0`、`--` 或示例值冒充状态。
- 支持 Dynamic Type、VoiceOver、深色模式和窄屏布局。

### 4. ActivityKit 基础

- 新增 Widget Extension，只接收精简内容状态，不自行联网或定位，并提供 ActivityKit 要求的全部系统展示形态。
- 用户显式开始/结束；App 驱动尽力而为更新并设置 `staleDate`。
- 适配锁屏和支持机型的 Dynamic Island；活动异常不得反向影响首页连接。
- 只编码真实可用字段；如果当前字段集合不足以形成有用的锁屏体验，允许延后完整展示增强，不以 fixture 补位。

### 5. 验证

- 单元测试：同源投影、局部降级、过期、隐私过滤、切设备隔离和 payload 大小。
- UI 测试：首页状态切换、开始/结束、禁用/失败提示与断线过期态。
- 真机：锁屏、Dynamic Island、App 前后台/终止、系统结束活动及约 8 小时活动生命周期边界。

## 不包含

- 未经官方文档、接口所有者授权或边界 ADR 批准的管理接口；网页 DOM 读取/注入、设备身份模拟或语音内容访问。
- 为填满仪表盘而前移完整 0.4 APRS 安全链或 0.8 QSO 导入。
- 0.3 中建设 APNs 推送后台或承诺锁屏持续实时刷新。
- 在锁屏携带精确坐标、完整事件数组、凭据或任何秘密。
- 在 0.3 前移 0.4 的 APRS 接入、收藏呼号/公共服务器或收藏事件过滤；0.3 只为未来可信事件保留投影槽位。

## 0.3 基础完成定义

- SPEC-014 中共享模型、GEO 支持字段、局部降级、隐私投影与无模拟回退具有自动化或明确真机覆盖；外部契约字段保持延期登记。
- 首页与锁屏由同一快照生成，来源和过期状态不会误导用户。
- ActivityKit 内容小于 4 KB，扩展无网络和定位客户端。
- 最终原型已经用户确认；Release 不含 fixture 或 demo provider，Simulator 构建及相关测试通过。
- 产品、技术、设计、模块文档和项目简报与实现一致。
- ADR-0005 字段计入 0.3 Release 完成度；白名单外字段继续按来源门槛延期。

## 官方参考

- [Apple：Displaying live data with Live Activities](https://developer.apple.com/documentation/activitykit/displaying-live-data-with-live-activities)
